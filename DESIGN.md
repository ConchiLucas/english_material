# English Material 配置中心设计规范

## 决策依据

这是一个供开发者维护项目数据源、AI 服务和本地 CLI 的高信息密度配置工作台。现有实现采用 React + Ant Design，并以深色侧栏、白色工作区和双栏配置编辑器承载核心任务。

候选参考评分（满分 5，按项目的配置工作台场景评估）：

| 候选 | 加权得分 | 结论 |
| --- | ---: | --- |
| Linear | 4.4 | 主参考：紧凑层级、克制状态色与键盘友好的工作台结构。 |
| Airtable | 4.0 | 不采用：适合数据对象管理，但其多色标签语言会削弱配置表单的聚焦。 |
| Sentry | 3.9 | 不采用：诊断密度适合运维，但对日常设置表单认知负担偏高。 |

## 视觉语言

- 主参考：Linear 的精确层级与克制强调；只借鉴原则，不使用其品牌、文案、字体或图形。
- 色彩：主操作 `#4f46e5`；危险操作使用语义红；侧栏 `#101828`；页面底色 `#f6f8fb`；卡片为白色。
- 排版：系统无衬线字体栈；正文优先保证中文可读性，代码/路径字段使用等宽字体。
- 间距：8px 基准；卡片圆角 8px；配置编辑器在桌面端为列表 + 详情的双栏结构。

## 交互与状态

- 选中项必须同时使用边框/背景与图标提示，不能仅依赖颜色。
- 所有删除操作需二次确认；保存、加载、空状态和错误状态必须有明确反馈。
- 表单字段提供可见焦点、必填校验、禁用态与安全的密码输入；API Key 读取后不回显。
- 窄屏（≤375px）将双栏改为单列；平板（≥768px）保留清晰操作区；桌面（≥1440px）维持高密度编辑器。
- 支持键盘 Tab 顺序、200% 缩放、减少动态效果，中文长文本换行不截断关键标签，并使用本地化日期格式。

## 有意偏差

- 为与现有功能保持一致，沿用 Ant Design 的圆角与控件行为；未引入参考站点的专有视觉资产或品牌标识。
