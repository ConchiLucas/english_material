---
version: alpha
name: English-Material-Workspace
description: "面向开发者的深色工作台：用紧凑层级、克制靛蓝强调色和明确状态反馈，安全地管理配置与英语词库。"

colors:
  primary: "#5E6AD2"
  primary-hover: "#7C86E8"
  primary-pressed: "#4E59B8"
  on-primary: "#FFFFFF"
  focus: "#9AA2FF"
  canvas: "#0B0D10"
  sidebar: "#0E1116"
  surface-1: "#13171D"
  surface-2: "#191E26"
  surface-selected: "#242946"
  hairline: "#2B323D"
  hairline-strong: "#424B59"
  ink: "#F4F6F8"
  ink-muted: "#B9C0CA"
  ink-subtle: "#8B95A3"
  disabled: "#656E7B"
  success: "#32B879"
  warning: "#D99A35"
  error: "#E85D68"
  info: "#5E9BE8"
  overlay: "rgba(0, 0, 0, 0.68)"

typography:
  page-title:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 28px
    fontWeight: 600
    lineHeight: 1.3
    letterSpacing: "-0.3px"
  section-title:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 20px
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "-0.1px"
  card-title:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 16px
    fontWeight: 600
    lineHeight: 1.5
    letterSpacing: "0"
  body:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.6
    letterSpacing: "0"
  label:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 14px
    fontWeight: 500
    lineHeight: 1.5
    letterSpacing: "0"
  helper:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif"
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "0"
  code:
    fontFamily: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', monospace"
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "0"

rounded:
  xs: 4px
  sm: 6px
  md: 8px
  lg: 12px
  full: 9999px

spacing:
  xxs: 4px
  xs: 8px
  sm: 12px
  md: 16px
  lg: 24px
  xl: 32px
  xxl: 48px

components:
  app-header:
    height: 64px
    backgroundColor: "{colors.canvas}"
    borderColor: "{colors.hairline}"
  sidebar:
    width: 264px
    backgroundColor: "{colors.sidebar}"
    padding: 24px 16px
  nav-item:
    minHeight: 44px
    rounded: "{rounded.md}"
    padding: 10px 12px
  panel:
    backgroundColor: "{colors.surface-1}"
    borderColor: "{colors.hairline}"
    rounded: "{rounded.lg}"
    padding: 24px
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    minHeight: 40px
    rounded: "{rounded.md}"
    padding: 8px 16px
  button-secondary:
    backgroundColor: "{colors.surface-2}"
    textColor: "{colors.ink}"
    borderColor: "{colors.hairline-strong}"
    minHeight: 40px
    rounded: "{rounded.md}"
    padding: 8px 16px
  input:
    backgroundColor: "{colors.canvas}"
    textColor: "{colors.ink}"
    borderColor: "{colors.hairline-strong}"
    minHeight: 40px
    rounded: "{rounded.md}"
    padding: 8px 12px
  status-badge:
    backgroundColor: "{colors.surface-2}"
    textColor: "{colors.ink-muted}"
    rounded: "{rounded.full}"
    padding: 2px 8px
---

# English Material 配置中心设计规范

## 1. 项目与证据

English Material 是一个本地英语材料工作台，管理数据库连接、AI 服务与本地 CLI，并通过已配置的数据源浏览去重单词表。主要用户是需要在本机维护配置和核对英语词库数据的开发者或操作人员。

本规范基于以下当前证据：

- React 18 + Ant Design 5 + Vite，核心页面集中在 `web-react/src/App.tsx`。
- 当前信息架构是固定顶栏一级导航与按工作区出现的二级导航；配置区包含数据库、AI 与 CLI 侧栏，词库区使用顶栏入口和无侧栏全宽内容。
- 当前样式只有一个 `767px` 断点，桌面布局密度适中，但移动端、200% 缩放和完整状态规范不足。
- 用户提供的运行截图是深色开发者工作台；源码中的 Ant Design 主题仍使用 light algorithm，视觉实现与截图存在偏差。
- 产品支持中文，字段中包含较长数据库地址、模型名称、命令路径和工作目录。
- 配置包含数据库密码、API Key 与本地可执行命令，错误或误操作成本高于普通内容编辑。

### 设计假设

本规范把用户截图中的深色体验视为目标方向，将“深色模式”定为首版默认。现有源码的浅色主题属于待实现差异，不在本次文档工作中修改。首版不承诺浅色模式；若未来增加主题切换，需要另行补齐等价的语义色，而不是对当前颜色机械反转。

## 2. 设计目标

1. **操作清楚**：用户始终知道当前配置类型、选中对象、未保存状态和操作结果。
2. **紧凑但不拥挤**：桌面端支持快速扫读和连续编辑，标签、辅助文案与危险操作仍有足够区隔。
3. **强调色克制**：靛蓝只用于主操作、当前选中和键盘焦点；状态使用独立语义色。
4. **默认安全**：密码和 API Key 不回显；删除、覆盖与测试失败都有明确恢复路径。
5. **中文优先**：布局按中文长标签、长错误信息、IME 输入和本地化格式设计。
6. **数据源隔离**：词库功能只读取用户选择的数据库配置，不修改外部材料数据源。

## 3. 三个 UI 参考与评分

评分采用 1–5 分，并按产品匹配 25%、信息架构与密度 15%、技术兼容 15%、可访问性 15%、响应式 10%、状态覆盖 10%、中文适配 5%、可转化性 5% 加权。总分为 100。

| 参考 | 产品 25 | 架构 15 | 兼容 15 | 无障碍 15 | 响应式 10 | 状态 10 | 中文 5 | 转化 5 | 总分 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Linear | 5 | 5 | 5 | 4 | 4 | 4 | 4 | 5 | **92** |
| Supabase | 5 | 4 | 4 | 4 | 4 | 4 | 3 | 4 | **84** |
| HashiCorp | 4 | 4 | 4 | 4 | 4 | 4 | 3 | 3 | **78** |

### 3.1 Linear — 主参考

- **5 分项**：开发者工作台定位与本项目高度一致；紧凑侧栏、表面层级和单一强调色可直接映射到 Ant Design；其原则易转化为项目自有组件规则。
- **4 分项**：参考文档的响应式与可访问性基础可靠，但主要来自营销表面，配置表单的错误、离线、权限和密钥状态仍需项目补全；中文需要放宽字距并增加标签宽度。
- **采用原因**：能让三类配置保持高密度、低装饰、强层级，不会让简单工具显得像大型云平台。

### 3.2 Supabase — 仅作局部次参考

- **5 分项**：数据库、密钥和开发者工具语境与本项目完全贴合。
- **4 分项**：方正控件、代码字体和数据库控制台结构适合连接配置；响应式和基础状态也可迁移。
- **3 分项**：原参考偏白色营销画布，品牌绿和英文短标签不适合直接移植到当前深色中文工作台。
- **使用边界**：只借鉴数据库连接测试、密钥遮罩、技术字段和资源状态的表达，不引入其绿色品牌色、营销排版或整套视觉身份。

### 3.3 HashiCorp — 不选为主参考

- **4 分项**：基础设施表单、权限语境、深色表面和明确语义状态与配置中心相容。
- **3 分项**：其多产品专属颜色、较重标题和企业营销节奏对仅有三项配置的小工具过度；完整转化后容易显得保守、厚重。
- **不采用原因**：本项目不需要用不同品牌色区分数据库、AI 和 CLI；统一强调色更利于建立稳定操作语义。

```yaml
reference_decision:
  primary:
    source: "VoltAgent/awesome-design-md: design-md/linear.app/DESIGN.md"
    ref: "8147538b4226ae41e2487a9179e3bcc1f68e8554"
    score: 92
    borrow:
      - "紧凑的工作区层级与侧栏导航"
      - "近黑画布、分层表面和细边框"
      - "单一且克制的靛蓝强调色"
      - "8px 控件圆角与少阴影的深度表达"
    adapt:
      - "使用项目现有靛蓝色系，不复制 Linear 品牌色关系"
      - "使用系统中文字体栈，不使用专有字体"
      - "把营销卡片规则改造成配置列表、编辑器和状态反馈"
  secondary:
    source: "VoltAgent/awesome-design-md: design-md/supabase/DESIGN.md"
    ref: "8147538b4226ae41e2487a9179e3bcc1f68e8554"
    score: 84
    scope: "仅用于数据库连接、密钥字段、技术值和连接测试状态"
    borrow:
      - "技术字段使用等宽字体"
      - "连接状态和密钥遮罩的清晰反馈"
  rejected:
    source: "VoltAgent/awesome-design-md: design-md/hashicorp/DESIGN.md"
    ref: "8147538b4226ae41e2487a9179e3bcc1f68e8554"
    score: 78
    reason: "多产品色彩身份和企业营销重量不符合单一配置中心"
  adapt_for_chinese:
    - "标题不使用激进负字距；中文正文保持零字距和 1.6 行高"
    - "表单标签允许换行，错误信息不截断"
    - "按钮宽度由内容决定，并为四至六字中文保留水平余量"
    - "路径、URL、模型 ID 在字符边界换行，并保留完整复制能力"
  avoid:
    - "第三方 Logo、品牌文案、专有字体和签名图形"
    - "把参考站点完整品牌身份复制到项目"
    - "渐变光晕、玻璃拟态、多强调色和无含义装饰"
```

### 来源与许可边界

三个参考均来自 [VoltAgent/awesome-design-md](https://github.com/VoltAgent/awesome-design-md)，固定提交为 `8147538b4226ae41e2487a9179e3bcc1f68e8554`，检索日期为 2026-08-04。参考文档采用 MIT 许可；许可不授予第三方商标、Logo、专有字体、图片或完整品牌身份的使用权。本项目只转化布局、层级和交互原则。

## 4. 项目视觉语言

### 4.1 色彩

- `canvas` 是全局背景，`sidebar` 只比画布略亮，避免导航成为独立大色块。
- `surface-1` 用于页面面板、配置编辑器和弹窗；`surface-2` 用于嵌套列表项、悬停和二级操作。
- `primary` 只用于主按钮、当前导航、选中配置和焦点相关提示；同一视口最多出现一个主要实心操作。
- 成功、警告、错误、信息必须使用语义色，并同时配合图标和文字，不得只靠颜色区分。
- 危险操作默认使用中性或描边样式；只有在确认上下文中才使用错误红色实心强调。
- 正文与背景达到 WCAG AA。辅助文字不得低于 4.5:1；大号或加粗文字至少 3:1。

### 4.2 排版

- 默认使用系统无衬线字体栈，优先保证 macOS、Windows 和中文字体的稳定呈现，不下载第三方专有字体。
- 页面标题 28px/600，区块标题 20px/600，配置对象标题 16px/600，正文与标签 14px。
- 中文不使用负字距；只有纯拉丁页面标题可使用 `-0.3px` 的轻微收紧。
- 数据库名、主机、端口组合、Base URL、模型 ID、CLI 命令与路径使用 `code` 字体，但字段标签和说明仍用正文体。
- 全大写只用于极短技术标识；中文界面不使用字母间距模拟视觉层级。

### 4.3 间距、形状与深度

- 以 4px 为最小单位，常用节奏为 8 / 12 / 16 / 24 / 32 / 48px。
- 表单字段之间 16px；逻辑分组之间 24px；页面标题与主体之间 32px。
- 控件圆角统一 8px，面板 12px，小标签 4–6px。按钮不得使用大胶囊圆角。
- 深度主要由表面色阶和 1px 边框表达。常规卡片不使用阴影；弹窗最多使用一层柔和阴影和遮罩。
- 选中项使用 `surface-selected`、主色边框和图标/勾选三者中的至少两种信号。
- 数据工作区遵循“边框减法”：一个区域只保留最外层弱边界，内部优先用表面色阶、留白和低对比度分隔线组织层级，避免出现三层以上同时可见的嵌套边框。

## 5. 信息架构与布局

### 5.1 全局结构

- 顶栏高 64px，左侧显示项目名“English Material”，右侧一级导航包含“配置管理”和“去重单词表”。不增加任务中心、任务列表或任务结果入口。
- 进入“配置管理”时，桌面显示 264px 二级侧栏，包含数据库配置、AI 配置和本地 CLI 配置；切换到“去重单词表”时完全移除侧栏。
- 配置主内容区最大宽度 1200px 并居中；去重单词表使用顶栏以下的全部可用宽度且不保留顶部空白。词表桌面水平外边距 32px，平板 24px，移动端 16px。
- 一级导航与配置二级导航分别维护选中态；从词表返回配置管理时恢复上一次选中的配置项。
- 配置页面标题区包含标题、简短说明和主操作。主操作在宽屏右对齐，在窄屏移到标题下方并占可用宽度；词表页不重复显示标题区，直接进入数据浏览工作区。

### 5.2 数据库配置

- 有数据时使用资源卡片或紧凑列表，优先展示名称、数据库类型、主机/数据库名、连接状态和是否默认。
- 新增与编辑使用同一表单结构：基础信息、连接地址、凭据、连接参数。高级参数默认折叠。
- “测试连接”是次操作，“保存”是主操作；测试结果显示在表单操作区附近，并包含失败原因与重试入口。
- 密码默认遮罩；用户主动输入的新密码仅在当前编辑期间可见，保存后不得从后端明文回填。

### 5.3 AI 与本地 CLI 配置

- 在 `≥1024px` 使用“对象列表 320px + 详情编辑器”的双栏布局；列表与详情各自有清晰标题。
- 在 `<1024px` 改为列表在上、编辑器在下；选中对象后滚动到编辑器标题并移动焦点，不使用窄小双栏。
- 默认项以“默认”标签、勾选图标和排序优先共同表达；“设为默认”是次操作。
- API Key、命令路径、默认参数和工作目录属于技术字段，允许横向滚动或字符边界换行，但必须提供完整复制能力。

### 5.4 去重单词表

- 页面自动优先使用“数据库配置”中名称或数据库名匹配 `rob_english_word` 的数据源，否则使用首个可用配置；页面不重复显示标题、说明、数据源选择和刷新区域。未配置时显示可恢复空状态，不猜测数据库地址。
- 主体采用“260px 层级目录 + 弹性表格”的工作区：目录按小学、初中、高中、大学、升学考试、商务与出国、专业、高阶考试和其他来源分组，并在子级显示册次或考试名称及实时词数。
- 层级目录默认展开当前选中分组；点击子级后在教材难度与来源难度之间保持单选，并把表格重置到第一页。分组和计数必须来自所选数据源返回的 facets，不复制参考项目的静态数量。
- 页面提供关键词、教材难度、来源难度和数值难度筛选；切换筛选条件时回到第一页。
- 桌面使用高密度表格展示单词、释义、难度、频率、来源和最佳例句；表格在自身容器内横向滚动，不引起页面级溢出。
- 单词和最佳例句音频共用单实例播放器，同一时间只播放一条；不可播放时按钮禁用并说明原因。
- 造句结果使用弹窗展示模型、句子、翻译和已有评分。页面不提供会调用外部生成服务或写入数据源的重新评分操作。
- 后端只允许针对选中数据源执行参数化 `SELECT`；禁止 `CREATE`、`ALTER`、`DROP`、`INSERT`、`UPDATE` 和 `DELETE`。

## 6. 组件规范

### 6.1 导航项

- 最小高度 44px，图标 16–18px，图标与文字间距 10px。
- 默认态用 `ink-muted`；悬停用 `surface-2`；选中态用 `surface-selected` + 主色边框或左侧指示条。
- 键盘焦点必须独立于选中态，使用 2px `focus` 外轮廓和 2px offset。

### 6.2 按钮

- 桌面最小高度 40px，触屏最小点击区域 44×44px。
- 每个操作组最多一个主按钮；新增、保存使用主按钮，取消、测试、设为默认使用次按钮。
- 图标按钮必须有可访问名称和 tooltip；加载中保留原宽度并阻止重复提交。
- 禁用按钮不能作为解释机制，旁边需说明为什么不可用或如何恢复。

### 6.3 表单

- 标签置于字段上方，必填星号与标签同一行；辅助说明紧随字段，错误信息取代辅助说明但不引起布局大幅跳动。
- 输入框最小高度 40px，触屏 44px；文本域允许垂直调整，Select 与 Input 高度一致。
- 密码/API Key 使用专用密码字段；复制动作复制真实值前必须由用户显式揭示或重新输入。
- 数字字段显示单位，例如“超时时间（秒）”；端口和超时只接受合理范围，并在失焦或提交时校验。
- 用户正在中文 IME 组合输入时，不触发即时保存、筛选或 Enter 提交。

### 6.4 配置列表项与卡片

- 首行是名称与状态，次行是可安全展示的摘要；不得在卡片上展示密码或完整 API Key。
- 悬停显示可点击性，选中状态保持稳定，不因鼠标离开而消失。
- 删除入口不与主操作相邻；弹出确认需包含对象名称和影响说明，默认焦点放在取消按钮。

### 6.5 弹窗与反馈

- 弹窗标题使用动作 + 对象，例如“新增数据库连接”；正文最大宽度 720px。
- 打开后焦点进入首个可编辑字段，关闭后回到触发按钮；Tab 焦点不得逃出弹窗。
- 成功 toast 简短说明结果；失败提示必须保留到用户处理，并给出字段级或页面级恢复路径。

### 6.6 数据表格与筛选

- 表头使用 `surface-2`，行使用 `surface-1`，悬停只做一级表面提升；排序方向同时用图标和 `aria-sort` 表达。
- 筛选区与表格只保留透明度约 45% 的 `hairline-strong` 外边界；表格行分隔线使用透明度约 55% 的 `hairline`，不使用强调性的纵向分隔线。
- 固定操作列与相邻数据列使用同一表面色，只用一条低对比度左分隔线标明边界，不使用深色墙面或投影遮罩。
- 筛选控件在桌面横向排列，在 768px 以下换行，在 375px 下单列且宽度占满。
- 长单词、释义和例句允许省略，但必须通过 title/tooltip 查看完整内容；技术数值不截断。
- 加载使用表格 skeleton 或明确加载行；空、失败和未选择数据源三种状态不得混用。

### 6.7 词库层级目录

- 分组行最小高度 40px，使用展开/折叠图标、组名和汇总词数；子项最小高度 44px，显示册次或考试名与实时词数。
- 目录面板保留 12px 圆角和弱外边界；分组行使用 `surface-2` 且默认无描边，分组与子项统一使用 6px 圆角，子项依靠缩进表达层级。
- 悬停只提升表面色；选中子项使用 `surface-selected`、左侧 3px 主色指示条和高对比计数徽标，不再使用完整主色描边。同一时间只允许一个教材或来源子项选中。
- 键盘焦点使用全局 2px `focus-visible` 外轮廓；选中态不得叠加完整边框形成双重轮廓。
- 目录自身纵向滚动，不推动页面产生额外横向滚动；折叠状态支持按钮键盘操作和 `aria-expanded`。

## 7. 状态规范

| 状态 | 表达规则 |
| --- | --- |
| 加载 | 页面首载使用与真实结构一致的 skeleton；单个保存只锁定当前表单，不遮挡整个应用。 |
| 空状态 | 说明“当前没有什么”、为什么需要它，以及唯一主操作；不使用占据大面积的纯装饰插图。 |
| 错误 | 字段错误贴近字段；服务错误显示原因摘要、重试入口和可复制的诊断信息。 |
| 成功 | 保存成功确认对象名称；测试连接成功显示时间和目标，不将“已保存”与“已连通”混为一谈。 |
| 未保存 | 页面标题或操作区显示“有未保存更改”；切换导航、对象或关闭页面前提示。 |
| 禁用 | 同时降低对比度并保留可读标签；提供禁用原因，不允许只变灰。 |
| 离线 | 顶部持久提示“当前离线”，禁止测试与保存；保留表单草稿并允许复制。 |
| 无权限 | 保留可读配置摘要，隐藏密钥值；说明所需权限和联系路径，不显示空白页。 |
| 密钥已保存 | 只显示固定长度遮罩和“已配置”；编辑时以新值覆盖，不从服务端回显原值。 |

## 8. 响应式规则

### 375px 窄屏

- 顶栏高 56px；保留项目图标，项目名可隐藏；两个一级入口使用“配置”“词表”短标签并保留完整可访问名称。
- 一级导航始终位于顶栏且不横向溢出；仅当处于配置管理时，三个配置项在顶栏下方显示为可横向滚动的二级导航，当前项始终可见。
- 去重单词表不显示二级导航，筛选器单列，表格只在自身容器内滚动。
- 词库层级目录位于筛选器和表格上方，使用限制高度的内部滚动区域，避免 260px 固定侧栏压缩内容。
- 内容边距 16px；页面标题 24px；按钮组纵向排列，主按钮占满宽度。
- 所有表单单列，弹窗变为距四边 12–16px 的近全屏面板；不得产生页面级横向滚动。
- 技术值可在字段内部滚动，并提供复制按钮。

### 768px 平板

- 配置工作区左侧导航宽 216px；去重单词表无侧栏；主内容边距 24px。
- 数据库卡片可两列，但表单保持单列或仅对短字段使用两列。
- AI/CLI 对象列表位于编辑器上方，避免 216px 导航加双栏编辑器造成三列拥挤。
- 去重单词表层级目录位于表格上方；从 1024px 起恢复 260px 目录与表格双栏。

### 1440px 桌面

- 顶栏 64px；配置工作区侧栏 264px、内容最大 1200px；去重单词表占满顶栏以下宽度。
- 数据库资源最多三列；AI/CLI 使用 320px 列表 + 弹性详情栏。
- 字段两列只用于语义成对且宽度可控的内容，例如配置 ID + 显示名称、模型 + 推理强度；长 URL 和路径跨满整行。

### 200% 缩放

在 1440px 浏览器以 200% 缩放时按约 720px 可用宽度处理：启用平板/窄屏折叠规则，表单保持单列，导航和弹窗无横向溢出，固定按钮不得遮挡内容。

## 9. 无障碍与本地化

- 所有交互支持键盘；视觉顺序与 DOM/Tab 顺序一致。列表选择可用 Enter/Space，方向键支持属于增强功能而非唯一方式。
- `:focus-visible` 使用 2px `focus` 轮廓和 2px offset；不得移除浏览器焦点而不提供替代。
- 图标与文字共同表达重要状态；装饰图标对辅助技术隐藏，独立图标按钮提供中文可访问名称。
- Toast、测试连接结果和字段校验使用合适的 live region；避免连续播报每次按键。
- 支持 `prefers-reduced-motion`：关闭位移动画和大面积过渡，只保留不超过 120ms 的颜色反馈。
- 中文标签允许两行但不拆开关键术语；错误详情按自然句子换行。日期时间使用 `zh-CN` 本地格式并包含时区语义。
- IME composition 期间不提交、不筛选、不触发快捷键；快捷键不得占用常用中文输入组合。

## 10. Do / Don't

### Do

- 用表面层级、边框和间距组织密度，保持页面安静。
- 让保存、测试、默认、删除四类动作拥有稳定且不同的视觉优先级。
- 对密码、API Key、连接失败和未保存更改给出明确状态与恢复路径。
- 在 375、768、1440 和 200% 缩放下验证真实中文长文本。
- 复用 Ant Design 组件行为，通过项目 token 统一外观。

### Don't

- 不增加项目配置、任务配置、任务列表、任务结果或任何任务中心功能。
- 不在当前服务或外部数据源中为去重单词功能执行建表、改表、迁移或数据写入。
- 不复制 Linear、Supabase 或 HashiCorp 的 Logo、专有字体、品牌文案、插图或完整视觉身份。
- 不为数据库、AI、CLI 分配三个装饰性品牌色；类型由图标、标题和内容识别。
- 不用渐变、玻璃拟态、大面积光晕、厚重阴影或过度圆角制造“科技感”。
- 不把危险操作放在主按钮旁，也不在未说明影响时直接执行删除。

## 11. 实施映射与已知差异

当前实现已按本规范映射到现有结构：

- `web-react/src/main.tsx`：Ant Design 主题算法和全局 token。
- `web-react/src/styles.css`：画布、侧栏、断点、焦点、缩放与 reduced motion。
- `web-react/src/App.tsx`：状态反馈、表单分组、未保存提示和响应式结构。
- `web-react/src/WordCleanPage.tsx`：数据源选择、筛选、分页、音频和造句结果。
- `src/main/java/com/aitaskcenter/service/WordCleanService.java`：基于数据库配置的参数化只读查询。

实施状态与后续验证项：

- 已使用 Ant Design `theme.darkAlgorithm` 和本文件的语义 token，实现深色默认主题。
- 已实现 375px 移动导航、768px 平板布局、1024px 列表/编辑器切换和 1440px 桌面布局。
- 一级入口已提升到顶栏；配置二级项按工作区显示，词表页移除侧栏并使用全宽内容区。
- 去重单词表已通过数据库配置选择数据源，并用 `rob_english_word` 的 22,415 条真实数据验证筛选、分页和造句结果。
- 去重单词表已复用参考项目的教材/考试层级目录交互，并改为读取当前数据源 facets 的实时分类与数量。
- 去重单词表已移除重复的页内标题、说明、数据源选择和刷新区域，进入页面后直接展示难度目录与单词表。
- 已实现加载、加载失败与重试、空状态、保存中、未保存提示、连接测试结果、危险操作确认和密钥不回显说明。
- 已在 375、720（200% 缩放等效宽度）、768 和 1440px 实机检查，无页面级横向溢出；已验证弹窗窄屏布局和可见焦点样式。
- 参考项目的“重新评分”动作依赖独立 Python 生成服务并会写入数据；为保证所选动态数据源严格只读，本项目只展示已有评分，不迁移该写操作。
- 离线和无权限状态缺少后端协议，当前仍未实现；这是功能能力边界，不以伪造前端状态代替。
- 仍需在真实读屏软件中验证播报顺序，并用实际中文 IME 完成一次组合输入测试。

## 12. 迭代规则

1. 新增颜色、字号、间距、圆角、阴影或组件变体前，先更新本文件。
2. 每次只处理一个可验证的组件或页面状态，避免在同一改动中重做全部视觉。
3. 任何偏离本规范的实现都要在本节记录原因、影响范围和回收计划。
4. 参考系统只提供方向；项目 token、组件名称、文案和交互语义必须保持 English Material 自有。

当前没有已批准的实施偏差。
