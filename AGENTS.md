# English Material

本项目仅包含配置中心：数据库配置、AI 配置和本地 CLI 配置。

- 启动：设置 `TASK_CENTER_DB_PASSWORD` 后执行 `./scripts/start-dev.sh`。
- 服务：React `19638`，Spring Boot `18744`。
- 数据库：本地 PostgreSQL 的 `english_material` 数据库。
- 不包含项目配置、任务管理、执行队列或 Python Worker。
